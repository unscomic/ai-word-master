# 英语单词 AI 学习助手 — 产品需求文档 (PRD)

## 1. 产品概述

### 1.1 产品定位
一款面向大学生和成人的 **AI 驱动英语单词学习 Web 应用**，覆盖四六级、考研、雅思、托福等考试场景。通过 AI 能力提供智能释义、记忆辅助、对话练习和个性化学习路径，帮助用户高效、持久地掌握英语词汇。

### 1.2 产品愿景
用 AI 替代传统"背单词书"的枯燥模式，让每个单词的学习体验都像有一位私人英语导师陪伴。

### 1.3 产品口号
"不只是背单词，是真正学会用单词。"

---

## 2. 目标用户

### 2.1 用户画像

| 画像 | 场景 | 核心痛点 |
|------|------|----------|
| 考研党 | 每天刷考研词汇，追求高效记忆 | 词量大、遗忘快、缺乏语境理解 |
| 四六级备考 | 考前突击，需要针对性训练 | 时间紧、不会用单词造句 |
| 雅思/托福考生 | 需要学术词汇+实际运用能力 | 单词认识但不会在口语/写作中用 |
| 英语爱好者 | 日常积累，想提升词汇量 | 缺乏系统和持续性，容易半途而废 |

### 2.2 用户核心需求
1. **高效记忆** — 不是死记硬背，而是通过 AI 生成的多维度信息深度理解单词
2. **对抗遗忘** — 基于遗忘曲线的智能复习提醒
3. **学以致用** — 能看到单词在真实语境中的使用
4. **学习反馈** — 知道自己的水平和进步
5. **持续动力** — 游戏化、打卡等机制保持学习习惯

---

## 3. 核心功能模块

### 3.1 功能总览

```
英语单词 AI 学习助手
├── 1. 用户系统
├── 2. 词库管理
├── 3. AI 单词学习（核心）
├── 4. AI 对话练习
├── 5. 智能复习系统
├── 6. 学习统计
└── 7. 个人设置
```

---

### 3.2 模块一：用户系统

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 注册/登录 | 邮箱注册 + 密码登录，支持 OAuth（Google/GitHub） | P0 |
| 个人资料 | 昵称、头像、目标考试类型（四六级/考研/雅思/托福） | P1 |
| 学习目标设置 | 每日单词数、考试日期、目标分数 | P1 |

### 3.3 模块二：词库管理

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 预设词库 | 内置常见词库：四级、六级、考研、雅思、托福核心词汇 | P0 |
| 自定义词表 | 用户可创建自己的单词列表 | P1 |
| 词库导入 | 支持 CSV/Excel 导入单词列表 | P2 |
| 词库搜索 | 搜索单词，查看是否在已学词库中 | P1 |

### 3.4 模块三：AI 单词学习（核心）

这是产品的核心模块。用户每次学习一个单词，AI 会生成以下内容：

| 功能 | 描述 | 优先级 |
|------|------|--------|
| **AI 智能释义** | 根据用户考试目标生成对应难度的中英文释义 | P0 |
| **多场景例句** | 生成 3-5 个真实场景例句（含中文翻译），覆盖日常/学术/考试场景 | P0 |
| **词根词缀拆解** | 自动分析单词的词根、词缀，帮助理解构词逻辑 | P0 |
| **记忆口诀** | AI 生成有趣、形象的记忆技巧或联想方法 | P0 |
| **同义词辨析** | 列出常见同义词，对比用法差异 | P1 |
| **常见搭配** | 展示高频词组搭配（collocations） | P1 |
| **发音** | TTS 朗读单词和例句 | P0 |
| **学习动作** | "认识"/"模糊"/"不认识" 三档标记，影响后续复习频率 | P0 |

**单次学习流程：**
```
展示单词 → AI 加载学习内容 → 查看释义、例句、词根、口诀
→ 标记掌握程度 → 进入下一个单词
```

### 3.5 模块四：AI 对话练习

| 功能 | 描述 | 优先级 |
|------|------|--------|
| **单词造句练习** | AI 引导用户用指定单词造句，给出语法和用法反馈 | P1 |
| **情景对话** | AI 创建包含当前学习单词的对话场景，用户参与对话 | P1 |
| **自由对话** | 与 AI 助手的开放式英语对话，练习口语/写作 | P2 |

### 3.6 模块五：智能复习系统

| 功能 | 描述 | 优先级 |
|------|------|--------|
| **间隔重复** | 基于 SM-2 算法的智能复习调度（1天/3天/7天/30天） | P0 |
| **每日复习提醒** | 推送今日待复习单词列表 | P0 |
| **错词强化** | 高频错误的单词进入强化模式，增加出现频率 | P1 |
| **复习检测** | 拼写测试、选择释义、听力辨词等多种检测方式 | P1 |

### 3.7 模块六：学习统计

| 功能 | 描述 | 优先级 |
|------|------|--------|
| **今日概览** | 今日已学/已复习/剩余单词数 | P0 |
| **学习日历** | 热力图展示每日学习活跃度（类似 GitHub 贡献图） | P1 |
| **进度曲线** | 已学单词数、掌握率变化趋势 | P1 |
| **考试预估** | 基于已学词库覆盖率估算考试准备度 | P2 |

### 3.8 模块七：个人设置

| 功能 | 描述 | 优先级 |
|------|------|--------|
| AI 温度调节 | 控制 AI 生成内容的创意程度 | P2 |
| 每日目标调整 | 修改每日新词学习数量 | P1 |
| 复习提醒开关 | 邮件/浏览器通知 | P2 |
| 主题切换 | 亮色/暗色模式 | P2 |

---

## 4. AI 能力矩阵

### 4.1 AI 调用场景汇总

| 场景 | AI 任务 | 输入 | 输出 | 触发方式 |
|------|---------|------|------|----------|
| 单词学习 | 生成学习卡片 | 单词 + 考试类型 | 释义、例句、词根、口诀 | 实时/预生成 |
| 造句练习 | 批改用户造句 | 句子 + 目标单词 | 语法纠正 + 用法建议 | 实时 |
| 情景对话 | 创建对话场景 | 单词列表 + 难度 | 对话文本 | 实时 |
| 自由对话 | 英语对话回复 | 用户消息 + 历史 | AI 回复 + 语法提示 | 实时 |
| 学习报告 | 生成周报总结 | 学习数据 | 总结文本 + 建议 | 定时/按需 |

### 4.2 Prompt 设计原则
- 每个 prompt 需包含用户考试目标（影响词汇难度）
- 输出格式需结构化（便于前端渲染）
- 控制 token 消耗（单词学习场景应精简高效）
- 内置安全过滤（拒绝执行非英语学习相关的指令）

### 4.3 AI 成本控制策略
- 高频调用场景（单词学习卡片）考虑**预生成 + 缓存**，避免重复调用
- 对话类场景使用**流式输出**提升体验
- 内置词库的常见单词优先使用预生成数据

---

## 5. 技术架构

### 5.1 推荐技术栈：Next.js 全栈方案

| 层 | 技术选型 | 理由 |
|----|----------|------|
| 前端框架 | Next.js 15 (App Router) + React 19 | 全栈能力、SSR 首屏快、路由系统完善 |
| UI 框架 | Tailwind CSS + shadcn/ui | 开发效率高、组件可定制、暗色模式支持好 |
| 状态管理 | Zustand | 轻量级、TS 友好、适合中型应用 |
| 数据库 ORM | Prisma | 类型安全、迁移管理、支持 PostgreSQL |
| 数据库 | PostgreSQL | 成熟稳定、支持全文搜索 |
| 缓存 | Redis | AI 内容缓存、会话管理 |
| 认证 | NextAuth.js (Auth.js v5) | 支持多种登录方式、开箱即用 |
| AI SDK | @anthropic-ai/sdk (Claude API) | 高质量文本生成 |
| TTS | Web Speech API / ElevenLabs API | 单词发音 |
| 部署 | Vercel + Docker | 一键部署、自动扩缩容 |
| 监控 | Sentry + PostHog | 错误追踪 + 用户行为分析 |

### 5.2 系统架构图

```
用户浏览器
    │
    ▼
┌──────────────────┐
│   Next.js App    │
│  ┌────────────┐  │
│  │  React UI  │  │
│  │ (shadcn/ui)│  │
│  └─────┬──────┘  │
│  ┌─────┴──────┐  │
│  │ API Routes │  │
│  │ (Server)   │  │
│  └─────┬──────┘  │
└────────┼─────────┘
         │
    ┌────┴─────┬──────────┐
    ▼          ▼          ▼
┌───────┐ ┌───────┐ ┌──────────┐
│PostgreSQL│ │Redis  │ │Claude API│
└───────┘ └───────┘ └──────────┘
```

### 5.3 数据模型（核心表）

```prisma
// 用户
model User {
  id            String    @id @default(cuid())
  email         String    @unique
  password      String
  name          String?
  avatar        String?
  examType      String?    // cet4/cet6/postgrad/ielts/toefl
  dailyGoal     Int       @default(20)
  createdAt     DateTime  @default(now())
  wordProgress  WordProgress[]
  sessions      Session[]
}

// 单词
model Word {
  id            String    @id @default(cuid())
  word          String    @unique
  phonetic      String?   // 音标
  aiContent     Json?     // AI生成的完整内容缓存
  wordbooks     WordbookWord[]
  progress      WordProgress[]
}

// 词库
model Wordbook {
  id            String    @id @default(cuid())
  name          String    // 四级/六级/考研...
  description   String?
  isPreset      Boolean   @default(false)  // 是否预设词库
  words         WordbookWord[]
}

// 词库-单词关联
model WordbookWord {
  wordbookId    String
  wordId        String
  word          Word      @relation(fields: [wordId], references: [id])
  wordbook      Wordbook  @relation(fields: [wordbookId], references: [id])
  @@id([wordbookId, wordId])
}

// 用户学习进度
model WordProgress {
  id            String    @id @default(cuid())
  userId        String
  wordId        String
  level         Int       @default(0)     // 0=未学, 1=模糊, 2=认识
  ease          Float     @default(2.5)   // SM-2 算法 ease factor
  interval      Int       @default(0)     // 复习间隔（天）
  repetitions   Int       @default(0)     // 复习次数
  nextReview    DateTime  @default(now()) // 下次复习时间
  lastReview    DateTime?
  mistakes      Int       @default(0)     // 累计错误次数
  user          User      @relation(fields: [userId], references: [id])
  word          Word      @relation(fields: [wordId], references: [id])
  @@unique([userId, wordId])
}

// 学习记录
model StudyLog {
  id            String    @id @default(cuid())
  userId        String
  wordId        String
  action        String    // learn/review/quiz/correct/wrong
  createdAt     DateTime  @default(now())
}

// 对话会话
model ChatSession {
  id            String    @id @default(cuid())
  userId        String
  type          String    // sentence_practice/scenario/free
  messages      Json      // [{role, content, timestamp}]
  createdAt     DateTime  @default(now())
}
```

---

## 6. UI/UX 设计原则

### 6.1 设计原则
- **渐进式展示** — 单词详情不要一次性全部展示，先出单词+发音，再逐步展开释义、例句、词根等
- **减少认知负荷** — 每次只聚焦一个单词，避免列表式压迫感
- **即时反馈** — 每次学习操作都有明确的视觉反馈
- **移动端友好** — 优先移动端体验（单词学习场景以移动端为主）

### 6.2 核心页面

| 页面 | 路径 | 说明 |
|------|------|------|
| 首页/Dashboard | `/` | 今日概览、学习进度、快捷入口 |
| 单词学习页 | `/learn` | 核心学习界面，卡牌式逐个学习 |
| 复习页 | `/review` | 间隔重复复习界面 |
| 词库浏览 | `/wordbooks` | 浏览和选择词库 |
| 对话练习 | `/chat` | AI 对话练习界面 |
| 统计页 | `/stats` | 详细学习数据 |
| 设置页 | `/settings` | 个人设置 |

---

## 7. 版本规划

### Phase 1 — MVP（核心可用）
- 用户注册/登录
- 预设词库（四级 + 考研）
- AI 单词学习卡片（释义 + 例句 + 词根 + 口诀）
- 基础复习系统（SM-2）
- 今日概览仪表盘
- TTS 发音

### Phase 2 — 体验增强
- 更多词库（六级 + 雅思 + 托福）
- AI 造句练习
- AI 情景对话
- 学习日历热力图
- 统计曲线
- 自定义词表

### Phase 3 — 深度优化
- AI 自由对话（流式输出）
- 学习周报
- 错词强化训练
- 拼写/听力测试
- 词库导入
- 暗色模式
- 移动端 PWA

### Phase 4 — 商业化
- 高级会员（更多 AI 调用额度）
- 学习计划定制
- 社区/排行榜
- API 对外开放

---

## 8. 成功指标

| 指标 | 目标值 |
|------|--------|
| 注册用户 7 日留存 | > 40% |
| 日均学习单词数 | > 15 |
| AI 内容满意度评分 | > 4.2/5 |
| 页面加载时间（LCP） | < 2.5s |
| AI 响应时间（P95） | < 3s |
